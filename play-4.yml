---
- name: play-4
  hosts: all
  become: yes

  tasks:

  - name: create directory project
    file:
      path: /Project13
      state: directory

  - name: create directory Ansible
    file:
      path: /Ansible13
      state: directory
